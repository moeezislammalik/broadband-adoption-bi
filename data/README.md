# Data layer

CSV files are generated by:

```bash
python scripts/generate_data.py
```

Commit the generated CSVs so Power BI users can open the model without running Python (optional — run script after clone if files are missing).
